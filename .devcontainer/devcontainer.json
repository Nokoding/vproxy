{
  "name": "vproxy-codespace",
  "image": "mcr.microsoft.com/devcontainers/rust:1",
  "onCreateCommand": "cargo install vproxy && cargo install bore-cli && sudo ln -sf \"$PWD/.devcontainer/restart-proxy.sh\" /usr/local/bin/restart-proxy && sudo apt-get update && sudo apt-get install -y zstd git-lfs && curl -fsSL https://ollama.com/install.sh | sh && (ollama serve > /tmp/ollama-setup.log 2>&1 &) && sleep 3 && ollama pull llama3.2:3b && curl https://get.acme.sh | sh -s email=nokodash311@gmail.com --nocron",
  "postStartCommand": "sh .devcontainer/postStart.sh"
}
